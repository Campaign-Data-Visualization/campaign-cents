mkdir -p $OPENSHIFT_DATA_DIR/config;
mkdir -p $OPENSHIFT_DEPLOYMENTS_DIR/current/config/;
cp $OPENSHIFT_DATA_DIR/default.json config/;
cp $OPENSHIFT_DATA_DIR/default.json $OPENSHIFT_DEPLOYMENTS_DIR/current/config/

